version: "3.9"

services:
  simulator:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: load-balancer-simulator

    ports:
      - "5000:5000"

    restart: unless-stopped

    environment:
      FLASK_APP: app.py
      FLASK_ENV: production

    volumes:
      - .:/app